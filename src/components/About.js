import React from 'react';
import { Component } from 'react';

export class About extends Component {
    render() {
        let title = 'About'
        return (
            <div>
                <h2>{title}</h2>
            </div>
        );
    }
}